export type PassCriteria = { requireAllAlpha?: boolean; maxNonAlpha?: number; maxTime?: number; minPoints?: number; minHitFactor?: number };
export type GoogleSheetField = "recordedAt" | "time" | "drill" | "course" | "alpha" | "charlie" | "delta" | "miss" | "nonAlpha" | "points" | "hitFactor" | "standard" | "result" | "runId";
export type GoogleSheetMapping = { sheetName: string; columns: Partial<Record<GoogleSheetField, string>>; startRow?: number; rowSpacing?: number };
export type CourseAttemptSheetMapping = { sheetName: string; columns: Partial<Record<GoogleSheetField, string>>; entryColumns: Record<string, Partial<Record<GoogleSheetField, string>>>; startRow?: number; rowSpacing?: number };
export type Drill = { id: string; name: string; description: string; maxHits: number; distance?: string; notes?: string; googleSheetUrl?: string; googleSheetMapping?: GoogleSheetMapping; libraryVisible?: boolean; standards?: Standard[]; passCriteria?: PassCriteria; favorite: boolean; active: boolean; createdAt: string; updatedAt: string };
export type Standard = { id: string; name: string; maxTime: number; metric?: "time" | "hitFactor"; description?: string; order: number };
export type CourseEntry = { id: string; drillId: string; order: number; standards: Standard[]; notes?: string };
export type Course = { id: string; name: string; description: string; notes?: string; googleSheetUrl?: string; googleSheetMapping?: GoogleSheetMapping; courseAttemptSheetMapping?: CourseAttemptSheetMapping; passCriteria?: PassCriteria; maxTotalNonAlpha?: number; minTotalPoints?: number; entries: CourseEntry[]; favorite: boolean; active: boolean; createdAt: string; updatedAt: string };
export type Run = { id: string; drillId: string; drillNameAtTime: string; courseId?: string; courseNameAtTime?: string; courseEntryId?: string; courseAttemptId?: string; sheetRow?: number; courseSheetRow?: number; timestamp: string; time: number; alpha: number; charlie: number; delta: number; miss: number; points: number; hitFactor: number; achievedStandardId?: string; achievedStandardName?: string; maxHitsAtTime: number; passCriteriaAtTime?: PassCriteria; passed?: boolean; failedCriteria?: string[]; syncStatus?: "pending" | "synced"; syncedAt?: string };
export type GoogleSheetsConnection = { spreadsheetId: string; spreadsheetUrl: string; sheetName: string; mapping?: GoogleSheetMapping; connectedEmail: string; createdAt: string; updatedAt: string };
export type AppSetting = { key: string; value: unknown };
export type Backup = { version: 1; exportedAt: string; drills: Drill[]; courses: Course[]; runs: Run[]; settings: Record<string, unknown> };
